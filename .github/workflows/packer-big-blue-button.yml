---
name: hetzner/big-blue-button

on:
  workflow_dispatch:
  push:
    paths:
      - "apps/hetzner/big-blue-button/**"
      - ".github/workflows/packer-big-blue-button.yml"
      - "apps/shared/**"

jobs:
  packer:
    runs-on: [self-hosted, Linux, X64]
    name: packer
    steps:
      - uses: ./.github/actions/build-image
        with:
          app: big-blue-button
