---
name: hetzner/gitlab

on:
  push:
    paths:
      - "apps/hetzner/gitlab/**"
      - ".github/workflows/packer-gitlab.yml"
      - "apps/shared/**"
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  packer:
    runs-on: [self-hosted, Linux, X64]
    name: packer
    steps:
      - uses: ./.github/actions/build-image
        with:
          app: gitlab
