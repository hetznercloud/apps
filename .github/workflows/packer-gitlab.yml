---
name: hetzner/gitlab

on:
  workflow_dispatch:
  push:
    paths:
      - "apps/hetzner/gitlab/**"
      - ".github/workflows/packer-gitlab.yml"
      - "apps/shared/**"

jobs:
  packer:
    runs-on: [self-hosted, Linux, X64]
    name: packer
    steps:
      - uses: ./.github/actions/build-image
        with:
          app: gitlab
