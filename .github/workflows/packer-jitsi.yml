---
name: hetzner/jitsi

on:
  push:
    paths:
      - "apps/hetzner/jitsi/**"
      - ".github/workflows/packer-jitsi.yml"
      - "apps/shared/**"
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  packer:
    runs-on: [self-hosted, Linux, X64]
    name: packer
    steps:
      - uses: ./.github/actions/build-image
        with:
          app: jitsi
