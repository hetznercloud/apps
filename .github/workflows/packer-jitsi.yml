---
name: hetzner/jitsi

on:
  workflow_dispatch:
  push:
    paths:
      - "apps/hetzner/jitsi/**"
      - ".github/workflows/packer-jitsi.yml"
      - "apps/shared/**"

jobs:
  packer:
    runs-on: [self-hosted, Linux, X64]
    name: packer
    steps:
      - uses: ./.github/actions/build-image
        with:
          app: jitsi
