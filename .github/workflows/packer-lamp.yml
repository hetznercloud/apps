---
name: hetzner/lamp

on:
  workflow_dispatch:
  push:
    paths:
      - "apps/hetzner/lamp/**"
      - ".github/workflows/packer-lamp.yml"
      - "apps/shared/**"

jobs:
  packer:
    runs-on: [self-hosted, Linux, X64]
    name: packer
    steps:
      - uses: ./.github/actions/build-image
        with:
          app: lamp
